# version 120

void main() {
    vec2 rect;
    vec4 color; // .r, .g, .b., .a
                // stuv
                // xyzw
                // [0], [1], [2], [3]
    gl_FragColor = vec4(1.0, 0.0, 0.0, 1.0);
}
